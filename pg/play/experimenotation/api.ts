/**
 * api.ts
 *
 * typescript api experimenotation
 */

import * as types from './api/types.ts'
import * as objects from './api/objects.ts'
import * as interfaces from './api/interfaces.ts'
import * as regex from './api/regex.ts'

const api = {
	types: types,
	objects: objects,
	interfaces: interfaces,
	regex: regex
}

export default api